#ifndef CONFIG_H
#define CONFIG_H

// #define CASE_INSENSETIVE

// print as write, str as string, bool as boolean
#define ALLOW_ALTERNATIVE_NAMES

// (a > b > c) instead of (a > b and b > c)
#define COMPARISON_CHAIN_SUPPORT

// note that comparison chains are always greed
// #define LAZY_EVALUATION

#endif // CONFIG_H
